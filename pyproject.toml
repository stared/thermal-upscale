[project]
name = "thermal-upscale"
version = "0.0.1"
description = "Compare AI super-resolution models on Thermal Master P3 frames."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=11",
    "numpy>=2",
    "matplotlib>=3.9",
    "requests>=2.32",
    "scipy>=1.13",
]
